version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
  build:
    commands:
      - echo Building the Spring Boot application...
      - mvn clean package
artifacts:
  files:
    - target/javaapp.jar